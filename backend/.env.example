# Backend environment configuration for CineMatch
TMDB_API_KEY=your_tmdb_api_key
OPENAI_API_KEY=your_openai_api_key

# Optional. Defaults to a local SQLite file (backend/cinematch.db) if unset —
# fine for local dev. Point at Postgres once this is deployed:
# DATABASE_URL=postgresql://user:password@localhost:5432/cinematch
